---
---

<canvas data-blockchain-canvas class="absolute inset-0 w-full h-full z-0"></canvas>

<script>
  import { initBlockchainCanvas } from "@/utils/blockchainCanvas";

  const canvasElement = document.querySelector<HTMLCanvasElement>(
    "[data-blockchain-canvas]",
  );

  if (canvasElement) {
    initBlockchainCanvas(canvasElement);
  }
</script>